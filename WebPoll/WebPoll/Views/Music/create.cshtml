@model WebPoll.Model.Models.Music

@{
	ViewData["Title"] = "create";
	Layout = "~/Views/Shared/_Layout.cshtml";

	string artistA = "band A";
	string artistB = "band B";
	string artistC = "band C";

	//artistA.Text = "band A";
	//artistA.Value = "band A";
	//artistB.Text = "band B";
	//artistB.Value = "band B";
	//artistC.Text = "band C";
	//artistC.Value = "band C";

	List<string> list = new List<string>();
	list.Add(artistA);
	list.Add(artistB);
	list.Add(artistC);

	SelectList artists = new SelectList(list);

	}

<h2>create</h2>

<h4>Music</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
			<div class="form-group">
				<label asp-for="Artist" class="control-label"></label>
				
			</div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

